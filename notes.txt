europython 
python -i file to import

@property
@prop.setter

type ('Foo', (), {}) is same as 
class Foo()

meta class is called at the class definition


@type_check
class Point(Base):
[...]

same as 

Point = type_check(Point)